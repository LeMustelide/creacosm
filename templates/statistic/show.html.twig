{% extends 'base.html.twig' %}

{% block title %}Statistic{% endblock %}

{% block body %}
    <h1>Statistic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statistic.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statistic_index') }}">back to list</a>

    <a href="{{ path('app_statistic_edit', {'id': statistic.id}) }}">edit</a>

    {{ include('statistic/_delete_form.html.twig') }}
{% endblock %}
