<form method="post" action="{{ path('app_question_mcq_single_delete', {'id': question_mcq_single.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question_mcq_single.id) }}">
    <button class="button button-danger">Supprimer</button>
</form>
